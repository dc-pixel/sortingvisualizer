# Contributing

Please keep algorithm changes isolated from UI changes where practical.

Before submitting a change:

- verify the visualizer still loads in a clean browser session;
- check every affected sorting algorithm;
- keep controls keyboard-accessible;
- update documentation for new algorithms or controls;
- keep commit messages focused and descriptive.

Examples: `feat: add heap sort`, `fix: handle empty array`, `style: improve mobile controls`.
